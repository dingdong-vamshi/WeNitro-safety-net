// Native uses the real system image picker's crop editor (allowsEditing).
export default function CoverEditor(_props: { uri: string; square?: boolean; onCancel: () => void; onSave: (uri: string) => void }) { return null; }
