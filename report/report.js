const pages=[...document.querySelectorAll('.page')];let current=0;const update=()=>{document.querySelector('#page-status').textContent=`${current+1} / ${pages.length}`};document.querySelector('#prev').onclick=()=>{current=Math.max(0,current-1);pages[current].scrollIntoView();update()};document.querySelector('#next').onclick=()=>{current=Math.min(pages.length-1,current+1);pages[current].scrollIntoView();update()};document.querySelector('#print').onclick=()=>window.print();const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){current=pages.indexOf(entry.target);update()}}),{threshold:.55});pages.forEach(page=>observer.observe(page));update();
